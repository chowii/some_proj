<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:orientation="horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:context="com.soho.sohoapp.feature.marketplaceview.feature.detailview.model.PropertyDetailHeaderItem"
                android:paddingTop="20dp"
                android:layout_marginStart="20dp"
                android:layout_marginEnd="20dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/user_avatar_view"
        android:paddingBottom="20dp"
        >
        <TextView
            android:id="@+id/title_text_view"
            style="@style/PropertyTitle"
            tools:text="Auction Guide $2.3m"
            />

        <TextView
            android:id="@+id/property_type_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Property Type | Property State"
            />

        <include layout="@layout/item_property_criteria" />

    </LinearLayout>

    <com.soho.sohoapp.customviews.UserAvatarView
        android:id="@+id/user_avatar_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"/>

    <View
        android:id="@+id/view_sepeperator"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/greyLightPrimary"
        android:layout_alignParentBottom="true"/>

</RelativeLayout>
